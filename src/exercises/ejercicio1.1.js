import { resultMessage } from "./_exercises"

const numero1 = 7
const numero2 = 5
const suma = numero1 + numero2

export function main1_1() {
    /* Usamos comillas invertidas para poder meter la variable dentro */
    resultMessage(`Number's summatory: ${suma}`)
}